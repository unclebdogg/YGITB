# Copy to .env locally; in GitHub, add these as Actions secrets instead.
OPENAI_API_KEY=sk-****
SLEEPER_LEAGUE_ID=123456789012345678
TIMEZONE=Australia/Sydney

# Optional: pin NFL season if you want (else autodetect via Sleeper state)
NFL_SEASON=2025

# Output config
OUTPUT_DIR=reports
REPO_COMMIT_AUTHOR_NAME=Fantasy Bot
REPO_COMMIT_AUTHOR_EMAIL=bot@example.com

# Schedule guardrails (local tz)
PREVIEW_DAY=Thursday
PREVIEW_HOUR=9        # 24h clock; run window logic inside script
RECAP_DAY=Wednesday
RECAP_HOUR=9
